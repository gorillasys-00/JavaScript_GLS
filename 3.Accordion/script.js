$(function () {
  // メインアコーディオンの動き（1つだけ開くようにする）


    // クリックされた見出し（.accordion-header）の次の要素（.accordion-content）を取得


    // 今クリックした要素の中身が開いているかどうかをチェック


    // 一旦すべての中身を閉じる

    // 全ての見出しの開いている状態をリセット（クラスや属性を削除）

    // アクセシビリティ用の属性を false に

    // 見た目を「閉じている」状態に戻す

    // 今クリックしたところが閉じていたなら、開く

    // 内容を下に開くアニメーション

    // アクセシビリティ用に「開いた」状態を指定

    // 見た目を「開いている」状態に


  // サブアコーディオンの動き（メインの中で1つだけ開くようにする）


    // クリックされたサブ見出しの次の要素（.sub-content）を取得


    // それが今見えているかどうかをチェック


    // 同じ親（ul）の中で、自分以外の .sub-content を全部閉じる

    // 一番近い <ul> を探す

    // その中の全ての .sub-content を探す

    // 今クリックしたもの以外を選ぶ

    // 他を閉じる

    // 自分以外の .sub-header の状態もリセット






      // すでに開いていた場合は閉じる





      // 閉じていた場合は開く



});